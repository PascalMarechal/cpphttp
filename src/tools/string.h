#pragma once

#include <vector>
#include <string>

namespace cpphttp
{
    namespace tools
    {
        std::vector<std::string> split(const std::string &s, char delimiter);
    }
} // namespace cpphttp
